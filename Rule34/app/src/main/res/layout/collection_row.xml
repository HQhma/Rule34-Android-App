<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="16dp"
    android:layout_marginHorizontal="8dp"
    android:background="@drawable/rounded_background"
    android:layout_marginTop="8dp"
    android:backgroundTint="@color/background_tag_color"
    android:id="@+id/collectionRowParent"
    >

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tag List Name"
        android:id="@+id/tagCollectionName"
        android:textColor="@color/text_color_white"
        android:fontFamily="sans-serif-black"
        android:textSize="20dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />
<!--

    <ImageButton
        android:id="@+id/shareBtn"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:src="@drawable/baseline_share_24"
        android:background="@android:color/transparent"
        app:tint="@color/text_color_white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/tagCollectionName"
        app:layout_constraintBottom_toBottomOf="@id/tagCollectionName"
        />

    <ImageButton
        android:id="@+id/editBtn"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:src="@drawable/baseline_edit_24"
        android:background="@android:color/transparent"
        app:tint="@color/text_color_white"
        app:layout_constraintEnd_toStartOf="@+id/shareBtn"
        app:layout_constraintTop_toTopOf="@id/tagCollectionName"
        app:layout_constraintBottom_toBottomOf="@id/tagCollectionName"
        android:layout_marginEnd="8dp"
        />
-->

    <!-- RecyclerView for Tags -->
    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:id="@+id/tagsRecyclerView"
        android:paddingVertical="8dp"
        app:layout_constraintTop_toBottomOf="@+id/tagCollectionName"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/searchBtn"
        />

    <!-- Search Button -->
    <Button
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Search"
        android:id="@+id/searchBtn"
        android:backgroundTint="@color/suggests_background_color"
        android:textColor="@color/text_color_white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tagsRecyclerView"
        />

    <!-- Delete Button -->
    <Button
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Delete"
        android:id="@+id/deleteBtn"
        android:layout_marginEnd="12dp"
        android:backgroundTint="@color/errorColor"
        android:textColor="@color/text_color_white"
        app:layout_constraintEnd_toStartOf="@+id/searchBtn"
        app:layout_constraintTop_toBottomOf="@id/tagsRecyclerView"
        />

<!--    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:background="@color/menu_divider_color"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@+id/searchBtn"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />-->
</androidx.constraintlayout.widget.ConstraintLayout>
